import { Box, Modal, ModalOverlay, ModalContent, ModalHeader, ModalCloseButton, ModalBody, VStack, Input, Textarea, ModalFooter, Button } from "@chakra-ui/react"

export interface Contact{
    id:number,
    name:string,
    phoneNumber:string
}

const Contact = () => {
  return (
    <>
      
    </>
  )
}

export default Contact